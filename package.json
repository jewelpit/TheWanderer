{
  "private": true,
  "scripts": {
    "postinstall": "dotnet tool restore",
    "start": "NODE_OPTIONS=--openssl-legacy-provider dotnet fable watch src --run webpack-dev-server",
    "build": "dotnet fable src && NODE_OPTIONS=--openssl-legacy-provider webpack"
  },
  "devDependencies": {
    "webpack": "^5.75.0",
    "webpack-cli": "^5.0.1",
    "webpack-dev-server": "^3.11.2"
  },
  "version": "1.0.0",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}